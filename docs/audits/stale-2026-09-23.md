# Stale docs/code scout — 2026-09-23

Wave 5 find-only inventory after Wave 3 (2026-09-22).
**No deletes. No renames. No product refactors.** This file is the only delivery.

Parent: WalksWithASwagger/wedges#38 · epic kk-kb#3693
Prior audit: [`docs/audits/stale-2026-09-22.md`](./stale-2026-09-22.md)

## Method

Read `docs/audits/stale-2026-09-22.md` first. Re-read every repo markdown file and
`public/llms.txt` on `main` at `044bc4d` (Wave 3). Compared issue/PR language
to live GitHub state on 2026-09-21 (tracker clock) / 2026-09-23 (this scout):

| State | Numbers |
| --- | --- |
| Open issues | #17, #19–#23, #38 |
| Closed since Wave 3 | #36 (Wave 3 scout; PR #37) |
| Still closed | #1, #9, #11, #13–#16, #18, #30, #32, #34 |
| Merged PRs cited here | #10, #12, #24–#28, #31, #33, #35, #37 |

**No Wave 4 / 4b wedges fix PRs exist.** Wave 3 (#36 / PR #37) reported zero new
findings, so there was nothing to repair. The only tree change since Wave 2c
(`8702d3b`) is this prior audit file (`docs/audits/stale-2026-09-22.md`). Product
code, configs, and current-state docs are unchanged.

Re-checked Wave 2b (`4293545` / PR #33), Wave 2c (`8702d3b` / PR #35), and Wave 3
(`044bc4d` / PR #37) against the D1–D12 and C1–C3 rows. Those repairs still
match the tree. Did **not** re-list them as current findings. Did **not**
reclassify C3: same evidence as Waves 1 and 3.

Checked internal relative links by file existence (decoded `%5B`/`%5D` path
segments; line-anchor targets for the release-readiness report). Grepped
`TODO`/`FIXME`, leftover “this PR / this branch / unmerged / invite-only”
current-state voice, `export` symbols vs importers, `package.json` scripts vs
`scripts/`, and `public/` leftovers.

**Not run (hard gates):** outbound HTTP, live `wedges.dev` probes, Vercel/Redis
admin, `npm audit`, secret inspection, paid evals.

## Counts

| Bucket | Wave 1 | Wave 3 | This scout |
| --- | ---: | ---: | ---: |
| New stale docs | 12 | **0** | **0** |
| New likely-dead code | 3 | **0** | **0** |
| p0 / p1 / p2 (new) | 0 / 8 / 7 | **0 / 0 / 0** | **0 / 0 / 0** |

**Zero new findings.** Trunk is clean of *new* stale-doc / dead-code rows after
Waves 2b/2c/3. Clean trunk is the expected and valid outcome: Wave 3 already
found nothing, and no product or doc work landed after that scout.

---

## Stale docs

No new rows. Closed/merged issue refs in current-state docs still use closed
language (`closed #14` / `#15` / `#18`; open `#17` / `#19` / `#20` / `#22`).
No internal markdown path 404s. Line anchors that Wave 2b repaired still land:

| Link | Target on `044bc4d` |
| --- | --- |
| `lib/club/store.ts#L364` | `isStoreConfigured()` |
| `lib/rate-limit.ts#L27` | `class MemoryStore` |
| `app/api/[transport]/route.ts#L137` | `checkRateLimit("critique", …)` |
| `lib/exercises/critique.ts#L20` | `generateObject({` |

Dated evidence sections that *record* old test counts, worktree ports, or the
#16 report’s own `git.deploymentEnabled` delivery exception remain historical
snapshots. Wave 2c already banners that leftover swarm-branch guards were
removed. Not re-listed.

Homepage footer still says rooms are share-link readable, matching
`docs/film-club.md` and open #19.

---

## Likely-dead code

No new rows. Re-verified Wave 1 “checked, not dead” and the leftover C3 types.

Do not treat appearance as proof of death. Each leftover export was checked
against imports, tests, scripts, MCP registration, and runtime entry points.

Same-file type/helper exports (`ENV_KEY_LIMIT`, `BYO_KEY_LIMIT`,
`findSelectorChoice`, `validatePng`, Club input types, visual-experiment
`SYSTEM` / `Runner`, and similar) are consumed inside their defining modules
or by live callers. They are not newly orphaned.

`scripts/eval-visual-critique.ts` is still unregistered in `package.json` on
purpose. Docs and `node --import tsx scripts/eval-visual-critique.ts --offline`
still consume it. Open #22 remains the product gate — left alone.

No `TODO`/`FIXME` in source or docs pointing at merged issues. No commented-out
blocks that reference removed features. Create-next-app `public/*.svg` files
remain gone. `vercel.json` still has no merged `codex/*` branch guards.

---

## Delta vs 2026-09-22

### Fixed (already on main before this scout)

D1–D12, C1, C2 remain the Wave 2b / 2c repairs. Wave 3 did not add Wave 4 / 4b
cleanup because it found nothing. Those earlier repairs are still correct; they
are not re-opened.

### Still open (not new)

| # | Path | Status after re-check | Wave 6 |
| --- | --- | --- | --- |
| C3 | `lib/rate-limit.ts` | `Bucket`, `RateLimitConfig`, and `RateLimitResult` are still exported with no importers outside this file. `checkRateLimit` / `ENV_KEY_LIMIT` / `BYO_KEY_LIMIT` remain live. Waves 2c and 3 left these types on purpose. Same evidence as Wave 1 — not reclassified. | verify-with-human |

### Newly found

None.

---

## Out of scope

Noticed, not turned into Wave 6 work:

- Live public-URL, firewall, Redis, or provider-spend verification (no outbound).
- Refreshing the 13-advisory `npm audit` snapshot.
- GitHub tracker #23 still recommending “assign #18” after #18 closed (tracker
  copy, not a repo doc). Film Club atomic work (#18 / PR #28) stays parked —
  KK chose option 3; Redis integration evidence is correctly documented as
  unverified on main and is not a stale-doc finding.
- Duplicate `AGENTS.md` / `CLAUDE.md` and the host-local Varlock doc path.
- `WEDGES_SITE_URL` used by CI/search-smoke but absent from `.env.schema`.
- Historical dated test counts and leftover PR-worktree voice inside
  implementation-evidence sections.
- Remaining real product gates: #17, #19–#22.
- Open #22 visual-review packet (parked experiment files are still consumed).
- Dark-crystal ceremony / booth ritual drafts (none in this trunk).
- Any delete, rename, or refactor of product code.

## Recommended next step

No new Wave 6 docs/code cleanup ticket is justified from this scout. Leave C3
and the open product issues for a human. Close this Wave 5 inventory once
reviewed.
