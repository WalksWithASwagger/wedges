# Stale docs/code scout — 2026-09-21

Wave 1 find-only inventory after the September 2026 issue/PR drain.
**No deletes. No renames. No product refactors.** This file is the only delivery.

Parent: WalksWithASwagger/wedges#30 · epic kk-kb#3671

## Method

Read every repo markdown file and `public/llms.txt`. Compared issue/PR language
to live GitHub state on 2026-09-21:

| State | Numbers |
| --- | --- |
| Open issues | #17, #19–#23, #30 |
| Closed issues | #1, #9, #11, #13–#16, #18 |
| Merged PRs cited here | #10, #12, #24–#28 |

Checked internal relative links by file existence (and line-anchor targets for
the release-readiness report). Grepped `TODO`/`FIXME`, commented-out removed
features, `export` symbols vs importers, `package.json` scripts vs `scripts/`,
and default `public/*.svg` references.

**Not run (hard gates):** outbound HTTP, live `wedges.dev` probes, Vercel/Redis
admin, `npm audit`, secret inspection, paid evals.

## Counts

| Bucket | Findings |
| --- | ---: |
| Stale docs | 12 |
| Likely-dead code | 3 |
| p0 / p1 / p2 | 0 / 8 / 7 |

---

## Stale docs

| # | Path | Why stale | Sev | Wave 2 |
| --- | --- | --- | --- | --- |
| D1 | `docs/review-release-readiness.md` | Line 18 still treats closed #14 as unmerged (“being removed… do not count an unmerged PR”). #14 closed; PR #24 merged `aa4e1e7`. The later merge-integration note contradicts this sentence. | p1 | update |
| D2 | `docs/review-release-readiness.md` | Line 47 still lists closed #15 as a remaining image gate (“#15/#22 retain…”). #15 closed via PR #25; only #22 is still blocked. | p1 | update |
| D3 | `docs/review-release-readiness.md` | Line 43 still present-tenses closed #18 as the concurrency packet. #18 closed via PR #28 (`7a0229e`, 2026-09-20). | p1 | update |
| D4 | `docs/review-release-readiness.md` | `[Store configuration checks](../lib/club/store.ts#L49)` lands on `ClubStore.get`, not config. `isStoreConfigured()` is at L364 after the #28 store rewrite. | p1 | update |
| D5 | `docs/VISION.md` | “Where we are” / “v1 shape (lightweight, no storage)” / “is the room part of v1?” still read as current. Film Club rooms, persistence, and `/club` shipped. | p1 | update |
| D6 | `docs/critique-evals.md` | Leftover PR voice: “The slice adds no website controls or responsive layouts.” `/review` and `/club` now exist. | p1 | update |
| D7 | `docs/browser-review.md` | “This PR must not be represented as deployed before release.” #11/#12 and #13/#27 merged; ROADMAP already records a production deploy of `dc75efe`. | p1 | update |
| D8 | `app/page.tsx` | Footer copy: “rooms are invite-only”. `docs/film-club.md` and open #19 say codes currently permit unauthenticated reads. | p1 | update |
| D9 | `docs/film-club.md` | “Result on this branch: unverified” is leftover PR-branch voice after #28 landed on `main`. | p2 | update |
| D10 | `README.md` | Repo map omits shipped `lib/review-storage.ts`, `lib/visual-critique-experiment.ts`, and `app/review/useReviewLibrary.ts`. | p2 | update |
| D11 | `public/llms.txt` | Creative AI Human Lab list omits Dark Crystal and unofficial.city, which `app/page.tsx` already links. | p2 | update |
| D12 | `ROADMAP.md` | “Shipped / Recent merged” stops at #24/#27. Atomic Club mutations (#18 / PR #28, 2026-09-20) are absent. | p2 | update |

No internal markdown path 404s. Dated evidence sections that *record* old test
counts (19 Node tests, 14/32 Chromium) are historical snapshots, not current
claims — not listed above.

Branch-guard sentences in `docs/browser-review.md`, `docs/critique-evals.md`,
and `docs/visual-critique-evidence.md` still name real `vercel.json` keys
(see C1). Misleading only as “this PR / during review” tense, not as missing
config. Fold those tense fixes into D6/D7 rather than extra rows.

---

## Likely-dead code

Do not treat appearance as proof of death. Each row checked imports, tests,
scripts, MCP registration, and runtime entry points.

| # | Path | Why likely dead | Sev | Wave 2 |
| --- | --- | --- | --- | --- |
| C1 | `vercel.json` | `git.deploymentEnabled` still lists six merged swarm branches: `codex/solo-critique` (#10), `codex/browser-review` (#12), `codex/issue-13-review-recovery` (#27), `codex/issue-14-club-attribution` (#24), `codex/issue-15-visual-experiment` (#25), `codex/issue-16-release-evidence` (#26). Harmless leftovers; they only matter if those names are reused. | p2 | delete |
| C2 | `public/file.svg`, `public/globe.svg`, `public/next.svg`, `public/vercel.svg`, `public/window.svg` | Create-next-app leftovers. Repo-wide grep: no `href`, import, or CSS `url()` references. `app/icon.svg` is the live icon. | p2 | delete |
| C3 | `lib/rate-limit.ts` | `Bucket`, `RateLimitConfig`, and `RateLimitResult` are exported but have no importers outside this file. `checkRateLimit` / `ENV_KEY_LIMIT` / `BYO_KEY_LIMIT` are live. | p2 | verify-with-human |

### Checked, not dead

| Path | Why it stays |
| --- | --- |
| `lib/visual-critique-experiment.ts`, `scripts/eval-visual-critique.ts`, `tests/fixtures/visual/*` | Parked #15 experiment. Unregistered on purpose; Node tests and offline CLI still consume it. #22 remains blocked. |
| `tests/fixtures/visual/generate.py` | Documented fixture author, not a runtime dependency. |
| Club `wouldShip` / legacy critique fields | Compatibility for stored rooms; tests and `docs/film-club.md` require them. |
| `checkRateLimit` on `POST .../submit` | Still invoked; it is a write limiter, not leftover generation. |
| `DEFAULT_MODEL` / `VISION_MODEL` | Same default string, but both are imported by live exercise runners. |

No `TODO`/`FIXME` pointing at merged issues. No commented-out blocks that
reference removed features.

---

## Out of scope

Noticed, not turned into Wave 2 work:

- Live public-URL, firewall, Redis, or provider-spend verification (no outbound).
- Refreshing the 13-advisory `npm audit` snapshot.
- GitHub tracker #23 still recommending “assign #18” after #18 closed.
- Duplicate `AGENTS.md` / `CLAUDE.md` and the host-local Varlock doc path.
- `WEDGES_SITE_URL` used by CI/search-smoke but absent from `.env.schema`.
- Historical dated test counts inside implementation-evidence sections.
- Remaining real product gates: #17, #19–#22.
- Any delete, rename, or refactor of product code.

## Recommended next step

Wave 2: one docs PR for D1–D8 (misleading current-state), then a separate
cleanup PR for D9–D12 + C1–C2 after a human confirms unused `public/*.svg`
and retired `codex/*` branch names will not be reused.
